<style>
    /* Full page height with a modern gradient background */
    .page-content-wrapper {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: background-color: #f4f4f4;
        padding: 20px;
    }

    /* Container for better responsiveness */
    .custom-container {
        width: 100%;
        max-width: 400px;
    }

    /* Glassmorphism card effect */
    .card {
        border: none;
        border-radius: 16px;
        backdrop-filter: blur(10px);
        background: rgba(255, 255, 255, 0.8);
        box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.15);
        overflow: hidden;
    }

    /* Styling the card content */
    .card-body {
        padding: 2rem;
        text-align: center;
    }

    .card-body img {
        width: 90px;
        margin-bottom: 15px;
    }

    .card-body h4 {
        font-size: 1.4rem;
        font-weight: bold;
        color: #333;
    }

    .card-body p {
        color: #555;
        font-size: 1rem;
        margin-bottom: 20px;
    }

    /* Beautiful button styling */
    /* Beautiful button styling */
    .btn-creative {
        display: inline-block;
        width: 100%;
        padding: 12px;
        font-size: 1rem;
        font-weight: 600;
        border-radius: 8px;
        background: linear-gradient(135deg, #6a11cb, #2575fc);
        /* Vibrant blue-purple gradient */
        color: #fff;
        text-decoration: none;
        transition: all 0.3s ease-in-out;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        border: none;
        cursor: pointer;
    }

    /* Hover effect */
    .btn-creative:hover {
        background: linear-gradient(135deg, #2575fc, #6a11cb);
        /* Reverse gradient on hover */
        transform: translateY(-3px);
        box-shadow: 0px 6px 14px rgba(0, 0, 0, 0.2);
    }
</style>

<div class="page-content-wrapper">
    <div class="custom-container">
        <div class="card">
            <div class="card-body">
                {{!-- <img src="img/bg-img/39.png" alt="Oops"> --}}
                <h4>OOPS... <br> It looks like you haven't subscribed!</h4>
                <p>Complete your profile and subscription to enjoy full access.</p>


                {{#if user}}
                <a class="btn-creative" href="/continue/{{user._id}}">Subscribe Now</a>

                {{/if}}
            </div>
        </div>
    </div>
</div>