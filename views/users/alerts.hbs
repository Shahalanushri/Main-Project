<!-- Add Font Awesome for Icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

<style>
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #f8f9fa;
    }

    .container {
        max-width: 900px;
    }

    .card {
        border-radius: 15px;
        border: none;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        background: white;
        padding: 20px;
    }

    .contact-card {
        border-radius: 12px;
        transition: all 0.3s ease-in-out;
        border: none;
        background: white;
        box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.1);
        overflow: hidden;
    }

    .contact-card:hover {
        transform: translateY(-4px);
        box-shadow: 0px 6px 14px rgba(0, 0, 0, 0.15);
    }

    .contact-icon {
        font-size: 22px;
        color: white;
        padding: 12px;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        display: flex;
        justify-content: center;
        align-items: center;
        transition: all 0.3s ease;
    }

    .contact-type-phone {
        background-color: #28a745;
    }

    .contact-type-email {
        background-color: #007bff;
    }

    .contact-type-other {
        background-color: #6c757d;
    }

    .contact-card .btn-danger {
        border-radius: 8px;
        transition: 0.3s ease;
    }

    .contact-card .btn-danger:hover {
        background-color: #dc3545;
        transform: scale(1.05);
    }

    .btn-dark {
        border-radius: 8px;
        font-weight: 500;
        transition: all 0.3s ease;
    }

    .btn-dark:hover {
        background: #343a40;
        transform: scale(1.05);
    }

    .alert {
        border-radius: 10px;
        padding: 15px;
        background: #ffe5e5;
        color: #b71c1c;
        font-weight: 500;
    }
</style>

<div class="page-content-wrapper p-3">
    <div class="container">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3">Send Alert To All</h4>
                {{!-- <a href="/add-contact" class="btn btn-dark btn-sm mb-4"><i class="fa-solid fa-plus"></i> Add
                    Contact</a> --}}

                <div class="card p-3">
                    <label for="">Send Alert Text</label>
                    <textarea id="alertMessage" class="form-control" placeholder="Enter alert message"></textarea> <br>
                    <button class="btn btn-danger" id="sendAlert">Send Alert</button>

                </div>
            </div>
        </div>
    </div>
</div>


<script>
    // ✅ Ensure socket connection
    const socket = io();

    document.getElementById("sendAlert").addEventListener("click", () => {
        const message = document.getElementById("alertMessage").value || "🚨 Default Admin Alert";

        fetch("/admin/send-alert", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ message })
        }).then(response => response.json())
            .then(data => console.log("Response:", data))
            .catch(error => console.error("Error:", error));
    });
</script>
<!-- FontAwesome Icons -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>