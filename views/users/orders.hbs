{{!-- <section>

    <div class="container my-5">
        <div class="table-responsive">
            <table class="table" id="table">
                <thead class="table-dark">
                    <tr class="align-middle">
                        <th scope="col">Date</th>
                        <th scope="col">Address</th>
                        <th scope="col">Pincode</th>
                        <th scope="col">Mobile</th>
                        <th scope="col">Amount</th>
                        <th scope="col">Payment</th>
                        <th scope="col">Status</th>
                        <th scope="col">Cancel</th>
                        <th scope="col">View</th>
                    </tr>
                </thead>

                <tbody>
                    {{#each orders}}
                    <tr class="align-middle">
                        <td>{{this.date}}</td>
                        <td>{{this.order.address}}</td>
                        <td>{{this.order.pincode}}</td>
                        <td>{{this.order.mobile}}</td>
                        <td>{{this.totalAmount}}</td>
                        <td>{{this.paymentMethod}}</td>
                        <td>{{this.status}}</td>
                        <td>
                            <a href="/cancel-order/{{this._id}}"
                                onclick="return confirm('Are You Sure to Cancel This Order ?')"
                                class="btn btn-danger">Cancel</a>
                        </td>
                        <td>
                            <a href="/view-ordered-products/{{this._id}}" class="btn btn-primary">View Product</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    {{else}}
    <div class="container mt-5">
        <div class="alert alert-secondary" role="alert">
            <h4 class="alert-heading">You Have Ordered Nothing!</h4>
            <hr>
            <p class="mb-0">You need to Order soemthing from the Shopping Cart/Cart.</p>
        </div>
        <div class="row">
            <a href="/" class="btn btn-primary">Add Products</a>
        </div>
    </div>
    {{/if}}
</section> --}}


<div class="container-md mt-3 p-5">
    <h4 class="mb-3">Your Event Bookings</h4>

    {{#if orders}}
    <div class="row">
        {{#each orders}}
        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body d-flex flex-column">
                    <div class="text-center">
                        <img src="/images/event-images/{{this.event._id}}.png" class="img-fluid rounded" alt=""
                            style="max-width: 100%; height: 200px; object-fit: cover;">
                    </div>
                    <h5 class="mt-3 text-truncate">{{this.event.title}}</h5>
                    <p class="text-muted">{{this.event.desc}}</p>
                    <div class="mt-auto">
                        <a href="/cancel-order/{{this._id}}"
                            onclick="return confirm('Are You Sure to Cancel This Booking ?')"
                            class="btn btn-danger">Cancel</a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    {{else}}
    <div class="alert alert-secondary text-center">
        <h4>You Have Booked Nothing!</h4>
        <p>Book an event now.</p>
        <a href="/home" class="btn btn-primary">Book Now</a>
    </div>
    {{/if}}
</div>

<div class="container-md mt-3 p-5">
    <h4 class="mb-3">Your Facility Bookings</h4>

    {{#if bookings}}
    <div class="row">
        {{#each bookings}}
        <div class="col-lg-4 col-md-6 mb-4">
            <div class="card h-100 shadow-sm">
                <div class="card-body d-flex flex-column">
                    <div class="text-center">
                        <img src="/images/event-images/{{this.event._id}}.png" class="img-fluid rounded" alt=""
                            style="max-width: 100%; height: 200px; object-fit: cover;">
                    </div>
                    <h5 class="mt-3 text-truncate">{{this.title}}</h5>
                    {{!-- <p>Price: ₹{{this.price}}</p> --}}
                    <p>Quantity: {{this.count}}</p>
                    <p>Date: {{this.date}}</p>
                    {{!-- <div class="mt-auto">
                        <button onclick="canelbooking('{{this._id}}')" class="btn btn-danger w-100">Cancel
                            Booking</button>
                    </div> --}}
                    <div class="mt-auto">
                        <a href="/cancel-booking/{{this._id}}"
                            onclick="return confirm('Are You Sure to Cancel This Booking ?')"
                            class="btn btn-danger">Cancel</a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    {{else}}
    <div class="alert alert-secondary text-center">
        <h4>You Have Booked Nothing!</h4>
        <p>Book a facility now.</p>
        <a href="/facilities" class="btn btn-primary">Book Now</a>
    </div>
    {{/if}}
</div>