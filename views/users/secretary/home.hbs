<style>
    .bg-overlay::after {
        position: absolute;
        content: '';
        background-color: #000000b5;
        opacity: 0.75;
        top: 0;
        left: 0;
        height: 100%;
        width: 100%;
        z-index: -1;
    }

    .cu-btn {
        border-radius: 100px !important;
    }

    .about-img {
        border-radius: 20px;
    }

    .ct-btn {
        font-size: 11px;
        border-radius: 50px;
    }

    .tiny-slider-one-wrapper .single-hero-slide {
        position: relative;
        z-index: 1;
        width: 100%;
        height: 200px !important;
        background-position: center center;
        background-size: cover;
    }

    .form-control.form-control-clicked {
        background-color: hsl(0, 31%, 95%);
        border-color: #c6c6c6;
        color: #073984;
    }
</style>

<div class="page-content-wrapper">

    <div class="container pt-5">
        <h4>Welcome to Secretary Hub</h4>

        <div class="pt-1 pb-4"></div>
        <h5>Services</h5>
        <hr>
        <a href="/all-facilities" class="btn btn-dark mb-3 w-100">Manage Facilities</a>
        <a href="/all-contacts" class="btn btn-dark mb-3 w-100">Manage Emergency Contacts</a>
        <a href="/all-announcements " class="btn btn-info mb-3 w-100">Manage Community Announcements</a>
        <a href="/all-events" class="btn btn-danger mb-3 w-100">Manage Events</a>
        <a href="/all-galleries" class="btn btn-dark mb-3 w-100">Manage Gallery</a>

    </div>
</div>

<script>
    function elementsSearch() {
        var input = document.getElementById('elementsSearchInput');
        var filter = input.value.toUpperCase();
        var list = document.getElementById("elementsSearchList");
        var listItem = list.getElementsByClassName('col-4');

        for (i = 0; i < listItem.length; i++) {
            var a = listItem[i];
            var textValue = a.textContent || a.innerText;
            if (textValue.toUpperCase().indexOf(filter) > -1) {
                listItem[i].style.display = "";
            } else {
                listItem[i].style.display = "none";
            }
        }
    }

</script>



</div>